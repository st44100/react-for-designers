import React from 'react';
import styled from 'styled-components';

const Heading = styled.h1`
  font-size: 24px;
  text-align: center;
`;

const Welcome = () => (
  <div>
    <Heading>Welcome to React + Storybook</Heading>
  </div>
);

export default Welcome;
